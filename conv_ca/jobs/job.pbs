#!/bin/bash -l

#PBS -N gridsearch
#PBS -l walltime=9:00:00
#PBS -l mem=2gb

source activate tf-cpu

cd $PBS_O_WORKDIR
JOB_DIR=$PBS_O_WORKDIR/..

cd $(JOB_DIR)
python3 conv_ca.py --num_layers=$(NUM_LAYERS) --state_size=$(STATE_SIZE) --run=$(RUN)
